services:
  postgres:
    image: postgres:16
    container_name: cars_postgres
    restart: always
    environment:
      POSTGRES_DB: carsdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4:8
    container_name: cars_pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - postgres

volumes:
  postgres_data:

#click dreapta pe Servers
#Register
#Server
#la General
#Name: cars-postgres
#la Connection
#Host name/address: postgres
#Port: 5432
#Maintenance database: carsdb
#Username: postgres
#Password: postgres

#Save.